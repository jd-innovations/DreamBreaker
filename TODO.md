# DreamBreakerPB — To-Do List
Derived from Audit_7_6.MD. Organized by priority (build order), not by phase number.

---

## 1. Phase 4 — Messaging (finish UI integration)

- [x] Wire mobile `conversation/[id].tsx` to Supabase realtime (`.channel(...).on('postgres_changes', ...)`) — done 2026-07-06 via `subscribeToConversation()` in `conversationService.ts`, wired into `RealDMScreen`, `RealTournamentGroupChat`, and the `community/[id].tsx` play-event chat tab. Manually smoke-tested and passed.
- [x] Closed two broken mock messaging entry points on mobile as part of the same pass: "Message Director" (`tournament/[id].tsx`) and "Message Organizer" (`community/[id].tsx`) no longer fall through to fake `dm-director`/`dm-organizer` screens when the real user id is missing — they now show a clear unavailable alert instead.
- [x] Wired web "Contact Director" (2026-07-10) — `tournaments/[id]/page.tsx`'s `contactDirector()` was a pure client-side toast, no DB write. Now opens the same in-page `MessagingPanel` modal already used for attendee "Send message" on this exact page (`tournament.director.id` was already loaded via the existing `profiles!director_id` join, no extra fetch needed), guarded with a friendly error if the tournament has no assigned director.
- [x] Reconciled web vs mobile inbox (2026-07-10) — audit found web's inbox isn't just behind mobile's, it only understands 1:1 DMs at all (no `conversation_type`/`conversation_participants` awareness, so group/tournament/play_event conversations are invisible on web; no `conversation_participant_settings`, so mute/archive/hide from mobile have zero effect there). Full parity would be a separate feature-sized web rebuild — scoped down per user decision to just the correctness bug: `panel.tsx`'s `startOrOpenConversation` was doing a raw `conversations` insert, bypassing the relationship-allowed check the `get_or_create_direct_conversation` RPC enforces (mutual match / director-registrant / play-event organizer-participant / etc — see the RPC migration for the full rule set). Now calls the same RPC mobile uses. The group/tournament visibility and mute/archive/hide gaps remain open web-only limitations, not fixed this pass.
- [x] Drop leftover debug-only SQL functions from the schema — already done: `20260701050000_fix_direct_dm_conversation_rls.sql:106-108` drops `debug_dm_organizer_check`, `debug_dm_full_check`, `debug_show_conversations_policies`, and no later migration recreates them.
- [x] Per-conversation mute + delete + archive, iOS Messages-style swipe actions on the inbox list — user request 2026-07-07 (mute/delete) and 2026-07-09 (archive, with a matching reference screenshot for both). Built across two passes:
  - Mute/delete (2026-07-07): `conversation_participant_settings` table (`muted_until`, `hidden_at`), swipeable row in `(tabs)/chat.tsx` via `Swipeable` from `react-native-gesture-handler`, `useUnreadCounts.ts`/`fetchConversations()` updated to exclude muted (badge) and hidden (list) conversations. Delete only hides the thread from your own inbox — shared `conversations`/`messages` rows are untouched.
  - Archive (2026-07-09): added `archived_at` column to the same settings table (migration `20260709010000_conversation_archive.sql`) rather than reusing `hidden_at`, since Archive needs to be recoverable via the Archived tab while Delete does not — confirmed with user before building. `fetchConversations()` takes a `mode: 'primary' | 'archived'` param; the previously-cosmetic "Archived" filter tab (existed in `FILTER_TABS` with no logic behind it) now actually switches which set loads. Archived threads are excluded from the unread badge (same treatment as muted) and do not auto-resurface on a new message, per user decision. Swipe row now shows Mute/Delete/Archive as circular icon badges (restyled 2026-07-09 to match a reference screenshot, replacing the original full-width solid-color rectangles).
  - Scope note: DMs and group conversations both use the same inbox list and same settings table, so both got mute/delete/archive together — no separate group-chat gating was needed.
- [x] Push notifications — server/DB half done 2026-07-08: `push_tokens` table (migration `20260708010000_push_notifications.sql`), a `notify_new_message()` trigger on `messages` insert that resolves recipient tokens (direct participant_a/b + `conversation_participants` for groups, excluding the sender and anyone with an active `conversation_participant_settings.muted_until`) and calls the `send-message-push` edge function via `pg_net`, which relays to Expo's push API. Verified end-to-end live: inserted a real message against a fake-but-valid-format token and confirmed the edge function returned 200 with Expo's real `DeviceNotRegistered` response, proving the full chain (trigger → pg_net → edge function → Expo) fires correctly. A flagged `SECURITY DEFINER`/RPC-exposure advisory on `notify_new_message()` was fixed same-day (`20260708010001_revoke_notify_new_message_rpc_access.sql`).
  - [ ] Client half still not built, deliberately deferred: no EAS project is configured for this app (no `eas.json`, no `projectId` in `app.json`), and on Expo SDK 54 remote push no longer works in Expo Go — needs a real EAS-linked dev build on a physical device plus Apple/Google push credentials, none of which can be provisioned without the user's EAS/Apple Developer account. Needs: `expo-notifications` install, `eas init`/`eas build:configure`, client-side token registration writing into `push_tokens`, and a dev-build device test before this is actually live end-to-end.
- [x] Group chat + DM photo attachments — built 2026-07-10, closing the previously-dead camera icon across the app. `messages` gained nullable `attachment_url`/`attachment_type` columns (migration `20260710000000_message_attachments.sql`), body's NOT NULL/check constraint relaxed to require either body or attachment. New `message-attachments` storage bucket (public read, upload gated via the existing `is_conversation_participant()` function so it covers every `conversation_type`). `conversationService.ts` got `uploadMessageAttachment()` (mirrors `uploadGroupPhotoFile` but fixes its `'image\jpeg'` MIME typo — not copied forward) and `sendMessage()`/`fetchMessages()` extended for the new columns. Wired the camera icon + image bubble rendering in the 3 real chat surfaces: `RealDMScreen` and `RealTournamentGroupChat` (both in `conversation/[id].tsx`) and `groups/[id]/chat.tsx` (the latter two had no camera icon at all before — added to match). Deliberately left alone: the 3 fully-mock chat variants in `conversation/[id].tsx` (`EventGroupChat`, `DMConversation`, `GenericDMConversation`) still have non-functional camera buttons — no real backend to wire on those screens. Confirmed live via direct query: columns/bucket/policies all present on `dreambreaker-pb`; one info-level security advisory noted (public bucket allows listing, not just fetch-by-URL) — same pre-existing pattern already accepted for `group-photos`, not a new risk.
  - Widened same-day (2026-07-10) from camera-only to camera / photo library / any file — user request, confirmed working on-device. `messages` gained an `attachment_name` column and `attachment_type`'s check constraint widened to `('image', 'file')` (migration `20260710020000_message_attachment_files.sql`). Installed `expo-document-picker`. New shared `lib/attachmentPicker.ts` (`pickImageFromCamera`/`pickImageFromLibrary`/`pickAnyFile`, each returning a common `PickedAttachment` shape with real `mimeType`/`fileName` from the picker result rather than guessed from the local cache URI) and `components/AttachmentOptionsSheet.tsx` (bottom-sheet action menu, same pattern as `groups/[id]/chat.tsx`'s existing `CurrentEventsSheet`) + `components/FileAttachmentRow.tsx` (tappable filename row for non-image attachments, opens via `Linking.openURL`). Tapping the camera icon in all 3 real chat surfaces now opens this sheet instead of jumping straight to the camera.
  - iOS bug caught + fixed via on-device diagnostics (2026-07-10): the sheet closed its `<Modal>` and launched the native picker on a 400ms timer, but iOS allows only one modal presentation at a time, so the picker silently no-op'd while the sheet was still dismissing. Fixed deterministically — `AttachmentOptionsSheet` now stashes the chosen action and runs it from the Modal's `onDismiss` callback (fires only after the sheet is fully gone) on iOS; Android runs it immediately (no such restriction, and `onDismiss` doesn't fire there). This "launch native picker/modal only from onDismiss, never on a timer" pattern is worth reusing anywhere else that presents a picker from within an RN Modal.
  - iOS permission strings added (2026-07-10): `expo-image-picker` config plugin with custom `photosPermission`/`cameraPermission` — but the fix had to go in **`app.config.js`, not `app.json`**. Gotcha discovered this pass: this project uses a dynamic `app.config.js` that spreads `app.json` but **fully replaces the `plugins` array**, so any plugin edit in `app.json` is dead config (silently ignored). All future plugin/permission changes must go in `app.config.js`. `expo-document-picker` intentionally NOT added — its plugin only sets iCloud entitlements (gated on `ios.usesIcloudStorage`); the document picker needs no Info.plist permission string. Verified via `npx expo config --type introspect` that the resolved native config now carries the custom `NSCameraUsageDescription`/`NSPhotoLibraryUsageDescription` (note: plain `expo config --json` shows them undefined because it doesn't run plugin mods — must use `--type introspect`). Takes effect on real dev-client/TestFlight/production builds; no visible change in Expo Go.
- [x] Messenger Dynamic Stories v0.1 — Real Activity Feed — built 2026-07-09 per the Dynamic Stories v0.1 spec. Replaced the previously fully-mock story strip in `(tabs)/chat.tsx` (hardcoded `STORIES` array, stock Unsplash photo, no `onPress` at all — see audit same day) with real system-generated cards:
  - Schema: `dynamic_stories` + `story_views` (migrations `20260709000001`/`20260709000002`), plus `profiles.location_lat/location_lng/home_court_id/story_radius_miles` (`20260709000000`) as a foundation for future ranking — capture UI (onboarding/location-settings write) is **not** built, these columns are currently unpopulated.
  - Generator: `generate_dynamic_stories()`, a pure-SQL `security definer` function (no edge function needed, unlike push — no third-party HTTP call), scheduled via `pg_cron` every 15 min. Verified live: manually invoked against the real project and got 1 real game story + 3 real tournament stories back with sane titles/spots-remaining/expirations; confirmed the cron job is registered and active.
  - Real story types: **Tournament** and **Game** (one shared row per source row, deduped via a partial unique index on `(source_type, source_id)`, refreshed in place each sweep) and **Partner** (shared skill-band rows — "N players near your level," not per-user rows; currently 0 rows since 0 profiles have `partner_preferences.actively_looking = true`, confirmed real via direct query, not a bug).
  - Placeholder-only per user decision: **Marketplace** and **Court Activity** bubbles render muted/disabled and show a "Coming Soon" alert on tap — no backing subsystem exists for either yet, so no fake data was built to feed them.
  - Client: `lib/storyService.ts` (fetch + categorize + client-side rank + mark-viewed), rebuilt story row in `(tabs)/chat.tsx` (gold ring = unviewed real content, muted ring = viewed/placeholder, synthetic client-only empty-state slide per category when a real category has zero live rows), and a new full-screen story viewer (`app/story/[category].tsx`, tap-to-advance slides + top progress bars + pinned CTA, registered as a `fullScreenModal` in `_layout.tsx`).
  - Caught and fixed one real bug pre-ship: the initial `story_views` upsert used PostgREST's default `ON CONFLICT DO UPDATE`, which needs an UPDATE RLS policy that was deliberately not granted (view rows are insert-once) — would have thrown a permission error on a *second* view of the same story. Fixed via `ignoreDuplicates: true` (`ON CONFLICT DO NOTHING`, which Postgres never gates on UPDATE privilege).
  - Not built, flagged explicitly rather than faked: no true distance ("near your area" language only, per user decision), no AI-generated copy (all slide text is templated from real fields), no location-capture UI to populate the new profile columns.
- [x] Message reactions — built 2026-07-10, full emoji picker per user decision (not just a preset tapback set). New `message_reactions` table (`message_id, user_id, emoji`, composite PK — migration `20260710010000_message_reactions.sql`), RLS mirroring `group_post_likes`'s shape but joining through `messages.conversation_id` + the existing `is_conversation_participant()` helper (no `conversation_id` column needed on the reactions table itself, so it works uniformly across every `conversation_type`). Added to the `supabase_realtime` publication; confirmed live via advisor scan, zero findings.
  - Client: new shared `lib/messageReactions.ts` (fetch/add/remove/subscribe) and `components/ReactionPills.tsx` (grouped emoji+count pill row, highlights the current user's own reactions, tap to toggle). Long-press on a bubble opens a full emoji keyboard (`rn-emoji-keyboard` — pure-JS, no native linking, picked over a preset-only tapback per user decision) to react with any emoji.
  - Wired into all 3 real chat surfaces: `RealDMScreen` and `RealTournamentGroupChat` (both `conversation/[id].tsx`) and `groups/[id]/chat.tsx`'s `TextMessage`. Realtime reaction updates are unfiltered at the Postgres level (the table has no `conversation_id` to filter server-side on) — each screen checks incoming rows against its own currently-loaded message ids client-side before applying, same tradeoff already accepted elsewhere in this codebase for similar cases.
  - Not done: `group_post_likes` (Feed posts) left as single-reaction "like" — widening it to multi-emoji was flagged as optional in the original note and wasn't part of this pass's scope (chat reactions only).

## 2. Phase 3 — Partner Finder (mobile: replace mock backend)

- [x] Correction from the original audit: `match/connections.tsx`, `match/requests.tsx`, `match/saved.tsx`, and `finder.tsx`'s `persistLike()` were already real (`partner_likes`/`partner_matches`, not `matchmaking_swipes`/`v_mutual_matches` — that's an older, web-only table pair). Only `import type` remained from `connectionStore` there.
- [x] Removed the two actual remaining mock-store dependencies, done 2026-07-06:
  - `finder.tsx` — deleted dead duplicate `sendConnectionRequest`/`savePlayer` calls that fired alongside the already-real `persistLike()`.
  - `match/profile/[id].tsx` — was 100% mock (`isConnected`/`hasPendingRequest`/`isSaved`/connect/bookmark). Rewired to real `partner_likes`/`partner_matches` queries and writes, mirroring the patterns already proven in `requests.tsx`/`connections.tsx`/`saved.tsx`.
  - `(tabs)/games.tsx` — pending-request badge count now a real `partner_likes`/`partner_matches` query instead of `getRequests()`.
  - `tournament/[id]/register.tsx` — "register with a connection" list now a real `partner_matches` + `profiles` join instead of `getConnections()`.
- [x] Filtered `useFinderCandidates.ts` by saved `partner_preferences` — but only what has a real backing field: `skill_ranges` (dupr-based) and `actively_looking` (excludes candidates who've turned off looking). `game_types`, `distance_idx`, `preferred_days/times`, `gender_preference` are **not** filterable yet — profiles has no comparable populated field for any of them (`looking_status`/`gender_category` aren't written by onboarding). Filtering on them would hide candidates based on data that doesn't exist, not their actual preferences.
- [x] Added compatibility scoring — ported web's `computeMatch()` into a shared `apps/mobile/src/lib/computeMatch.ts` (DUPR proximity + availability match, distance tier dropped since mobile has no real distance). Surfaced as a "% Match" badge on the finder swipe card and profile detail screen.
- [ ] Real distance calculation — deferred by design decision (2026-07-06): needs a `profiles.location_lat/location_lng` migration + onboarding capture, neither of which exist today. Revisit as its own follow-up.
- [x] Groups feature rebuilt on a real Supabase backend (2026-07-07) — was 100% mock (`groupStore.ts` in-memory, `groupStore.ts` deleted). New tables: `groups`, `group_members`, `group_posts`/`group_post_likes`/`group_post_comments`/`group_poll_options`/`group_poll_votes`, `group_photos`, plus `play_events.group_id` (real events now link back to their group) and a `group-photos` storage bucket. Group chat reuses the existing contextual-conversations infra (`conversation_type='group'`). The Members tab Connect button now calls real `partner_likes` (`lib/partnerLikes.ts`) instead of the old mock `connectionStore`. Feed/Photos/Events/Members/chat are all real; Pin Group and Manage Notifications remain "coming soon" stubs (no such infra exists elsewhere in the app either). Migrations: `20260707000001_groups.sql`, `20260707000002_group_photos_bucket.sql`, `20260707000003_group_photos_bucket_policy_fix.sql`.
- [x] Confirmed via live Supabase check (2026-07-07): all 4 Partner Finder migrations (`partner_preferences`, `partner_likes`, `partner_matches`, `profiles_date_of_birth`) are applied on `dreambreaker-pb` (project `fbzetvkbhneptvfruilw`). `conversations` insert RLS exists and is more complete than TASK_PARTNER_FINDER.md describes — covers direct DMs, mutual matches, director/registrant DMs, play-event, and tournament/announcement conversations (see `20260701050000`/`20260701060000`). TASK_PARTNER_FINDER.md's "Pending push" status line is stale and should be updated separately.

## 3. Phase 2 — Community Play (remove mock fallback risk)

- [x] Correction from the original audit (2026-07-06): the `UUID_RE.test(id)` / `isSupabase` branches are **not** a live silent-data-loss risk. All three creation flows (`create-quick-game.tsx`, `create-round-robin.tsx`, `create-mini-tournament.tsx`) always call the real Supabase create function and always navigate forward with the resulting real UUID — every downstream screen (`*-created`, roster, schedule, score-entry, standings, results, bracket) receives a real id in the actual product flow. The mock/local-store branch in those screens is dead code in that flow; it's only reachable via `groups/[id].tsx`'s mock event routing (a separate mock feature, left alone per the Partner Finder pass) or a manual deep link with a non-UUID id.
- [x] Removed 2 confirmed-dead unused imports left over from before these screens were wired to Supabase: `setRoundRobin` (`create-round-robin.tsx`) and `setMiniTournament` (`create-mini-tournament.tsx`) — imported, never called.
- [ ] Decided (2026-07-06): leave the unreachable legacy fallback branches in `roster.tsx`/`schedule.tsx`/`score-entry.tsx`/etc. as-is for now — removing them touches ~14 files for no user-facing benefit while they're already unreachable in the real flow. Revisit only if/when the mock Groups feature is made real (that's the one thing still routing into them).
- [ ] Decide whether web needs feature parity for Round Robin / Mini Tournament UI, or whether mobile-only is the intended permanent scope. (Not evaluated this pass — mobile-focused.)
- [x] Wired the `/invites` screen fully to real `play_event_invites` data (2026-07-09) — was a mix of real (only the top "Game Invites" pending section) and hardcoded mock (`RECEIVED_NEW`/`RECEIVED_OLDER`/`SENT` arrays with fake Unsplash avatars and dates like "May 24, 2026"). All mock arrays removed:
  - Received tab: pending invites (actionable Accept/Decline, unchanged) plus a real "Older" history section (accepted/declined/cancelled) from the same `fetchReceivedInvites()` call, split client-side by status.
  - Sent tab: added `fetchSentInvites()` (`lib/supabase/playEventInvites.ts`) — was entirely unbuilt, the old Sent tab only read the in-memory (non-persisted) `inviteStore.ts`. Now shows real sent invites with a working Cancel action on pending ones, wired to the already-existing-but-previously-unused `cancelPlayEventInvite()`.
  - Removed navigation to the mock `/invite-detail` screen for these rows — that screen operates on the old mock `Invite` shape/ids and has no real counterpart, so real rows are now display-only instead of linking somewhere broken.
  - [ ] Still separately mock, not touched this pass: `players/[id]/invite.tsx` → `invite-details.tsx` → `invite-sent.tsx` (the "Invite to Play" flow reached from a player's profile, with Community/Tournament/Team/Practice options) only writes to `inviteStore.ts` (in-memory, resets on app restart) and never touches `play_event_invites`. It doesn't map cleanly onto the real schema — `play_event_invites` only models "invite to a specific play event," not the Tournament/Team/Practice categories this flow offers — so wiring it for real needs a product decision on whether to redesign it as an event-picker (invite a friend to one of *your* upcoming events) or drop the non-community categories. Left as-is pending that decision; its writes no longer leak into the real Sent tab (fixed above), so at least it's no longer polluting real data with fake rows.
- [ ] Push notifications for game invites — not built, and deliberately not started: the client-side push infrastructure itself doesn't exist yet (see Phase 4 Messaging push item above — no EAS project configured, no `expo-notifications`, no dev build). Once that groundwork lands for messages, extend the same `push_tokens`/edge-function pattern to `play_event_invites` (a `notify_play_event_invite()`-side trigger already exists but only writes to the in-app `notifications` table, not push).

## 4. Phase 6 — Director Platform

Ordered by build sequence: quick de-mocking wins first (no schema), then read-only Stripe/reports surfaces (schema exists). Dashboard already loads **real** tournaments via `fetchDirectorTournaments(user.id)` — remaining "mock" is narrower than the original audit implied. Staff/role assignment is **out of scope** (descoped 2026-07-12 per user decision).

### 4a. Dashboard de-mocking (small, no schema, do first)
- [x] Replaced hardcoded `DIRECTOR_NAME = 'Mike Johnson'` + `"MJ"` avatar initials (2026-07-12) — `director.tsx` now uses `useProfile()` (already existed, `hooks/useProfile.ts`) for `full_name`-derived initials and name. "Verified" badge now reflects real `director_status` (`approved` → Verified, `pending` → Pending, `suspended`/null → hidden) instead of always-on.
- [x] Relocated `Tournament` type off `@/data/mock` (2026-07-12) — moved to new `@/lib/tournamentTypes.ts`; `@/data/mock` now only holds seed rows (`MOCK_TOURNAMENTS`, `Player`). Updated all ~22 import sites across the tree. `tsc --noEmit` and `expo export` both clean (one pre-existing, unrelated `(tabs)/index.tsx` type error left untouched — not caused by this change, tracked separately).

### 4b. Payout visibility — DEFERRED (2026-07-12 per user decision)
- Investigated: Stripe Connect onboarding (bank-account linking) is real but web-only (`web/src/app/api/stripe/connect/*`); mobile has no Stripe integration at all. Onboarding status fields (`profiles.stripe_connect_account_id`/`stripe_connect_onboarded_at`) are genuinely synced.
- However `registrations.entry_fee_paid_cents` (what `director.tsx`'s `metrics.revenueCents` sums) is written directly by the mobile client (`lib/supabase/registrations.ts:311-322`) — no Stripe PaymentIntent is ever created or confirmed. `stripe_entry_intent_id`/`stripe_hold_intent_id` columns exist on `registrations` but are unused. No payout/transfer data exists anywhere.
- Decision: don't build a payout UI over numbers with no real money behind them. Revisit once real payment capture lands (see cross-cutting item: "Add real payment processing behind 'Hold My Spot'").

### 4c. Tournament reports / export
- [x] Director-facing report screen (2026-07-12) — new `tournament/[id]/report.tsx` + `lib/tournamentReport.ts` (`fetchTournamentReport()`). Shows registration/check-in/waitlist/no-show summary, per-division breakdown, and full roster with per-player amount + status. "Entry Fees Recorded" is explicitly labeled and footnoted as not-yet-real captured money (same finding as 4b — no payment capture exists yet), to avoid implying otherwise. Linked from a new "Tournament Report" quick action in `command-center.tsx`.
- [x] CSV export (2026-07-12) — installed `expo-sharing@~14.0.8` + `expo-file-system@~19.0.23` (SDK-54-compatible via `npx expo install`). `exportRosterCsv()` writes the roster to a cache file and opens the native share sheet. Wired into both the report screen's Export button and `command-center.tsx`'s previously-stubbed "Export Players" quick action (was a `comingSoon()` alert). PDF/other formats not built — roster CSV only, per original scope note.

### 4d. Director application flow + real tournament creation (2026-07-12, user-requested)
Triggered by: "can a user register as a director in mobile app?" — audit found no self-serve path existed anywhere in mobile, and the CTA a player *would* hit (Director Hub, shown to everyone) led to a dead end.

- [x] **Security fix, predates this work**: `profiles: own update` RLS only pinned `role`, never `is_director`/`director_status` — any authenticated user could self-approve as director via a raw REST call. Closed in migration `20260712000000_director_application_rpc.sql` (applied live to `dreambreaker-pb`), which also adds `apply_to_be_director()` (SECURITY DEFINER RPC, the only legitimate self-service path onto the approval queue — always lands on `pending`, blocks re-use by suspended directors). Regenerated `database.types.ts` afterward.
- [x] New `apply-director.tsx` screen — real write via the RPC above, shows live pending/approved/suspended state.
- [x] `profile.tsx`'s "Director Hub" row (previously shown to *every* signed-in user, director or not) now branches on real `director_status`: not-applied → "Become a Director", pending → "Director Application", suspended → "Director Access", approved → "Director Hub".
- [x] `director.tsx` gated: non-directors and pending/suspended directors now see an explicit status screen instead of the live dashboard (previously any player reaching this screen via the ungated nav saw the same empty-state "Create Your First Tournament" CTA as a real director).
- [x] **Second bug found underneath**: `director/create-tournament.tsx` called `createTournament()` from `directorTournamentStore.ts` — a 100% local mock store, zero Supabase calls. It fabricated a fake tournament ID and routed to `command-center.tsx`, which loads real Supabase data and would have failed to find it. This meant tournament creation was broken for *any* user, director or not, independent of the access-gating bug. Fixed: `directorTournamentStore.ts` deleted (confirmed zero remaining references); `create-tournament.tsx` now calls a real `createDraftTournament()` (`lib/supabase/tournaments.ts`), inserting a real `status: 'draft'` row. Added free-text date parsing (`parseFormDate()`) since the form takes strings like "Aug 16, 2026" with no date picker — previously these would have hit a raw Postgres date-cast error had the insert been real.
- [x] Real draft → submit-for-approval → admin-approves lifecycle wired to mirror web (`web/src/app/director/page.tsx`'s `submitForApproval`): `command-center.tsx` gained a draft banner + "Submit for Approval" action (`submitTournamentForApproval()`). Paid tournaments (`entryFeeCents > 0`) are blocked with a message to complete Stripe Connect onboarding on web first — mobile has no Connect UI (see 4b), so this mirrors web's own payout gate rather than faking one.
- [x] **Third bug found while wiring this up**: `dbStatusToAppStatus()` collapsed both `draft` and `pending_approval` DB statuses into the generic `upcoming` app status, and `getTournamentStatus()` then defaulted anything unrecognized to `'open'` — so a director's own unsubmitted draft would have rendered with a green "Open" chip, as if players could already register. Fixed: both statuses now pass through as their own literal `TournamentStatusKey`s (`draft` → gray "Draft" chip, `pending_approval` → gold "Pending Review" chip).
- [x] Added `stripe_connect_onboarded_at` to the shared `UserProfile` type/select (`lib/services/profile.ts`) to support the paid-tournament submit gate above.

## 5. Phase 5 — Facilities Directory

**Status (confirmed 2026-07-12): frozen, real and stable.** No new feature work planned per the original audit's freeze recommendation — revisit only if one of the three gaps below becomes a real user complaint. Re-verified against current code this pass (no facility-related work has happened since the 2026-07-06 audit; all recent sessions touched Messaging/Partner Finder/Director instead).

- [x] Facility detail/picker/card — real, reused in tournament & event creation flows (`FacilityPicker`, `facility/[id].tsx`).
- [ ] Nearby/map tournament-pin layer is hardcoded — the courts tab itself is real, but tournament pins on the map are not driven by live data.
- [ ] Data population is a one-time static Google Places import (`20260701004107_google_places_facilities_import.sql`), not a live/refreshing pipeline — confirmed no `pg_cron` or scheduled job attached to it.
- [ ] No admin moderation surface for facility claim/verification, despite the backend already supporting it (`20260629000002_facilities_claim_and_rpc.sql`) — same underlying gap as the "Build facility moderation admin surface" item under Phase 7 below (one fix would close both).

## 6. Phase 7 — Admin Platform

- [ ] Build facility moderation admin surface (claim/verification support already exists in migrations, no UI).
- [ ] Replace the randomized "Platform Growth" chart and hardcoded KPI trend percentages with real computed analytics.
- [ ] Make admin "Communications" actually send (currently only updates local component state, not dispatched via email/notification service).

## 7. Cross-cutting / Foundation cleanup

- [x] Mobile Google OAuth — implemented and wired 2026-08-02–2026-08-05 (see `TASK_GOOGLE_SIGNIN.md` for full detail). Browser-redirect flow (`expo-auth-session` + `signInWithOAuth` + `WebBrowser.openAuthSessionAsync` + `setSession`) via new `signInWithGoogle()` in `lib/auth.ts`, wired into both `onboarding/create-account.tsx` and `sign-in.tsx`. Google provider verified enabled with real credentials on the live `dreambreaker-pb` project. Web Google sign-in **not** addressed — mobile only. Apple remains an explicit unimplemented stub.
  - [ ] Real-device OAuth test never run (step 7 of the task doc) — Expo Go can't reliably handle the redirect; needs a dev build. Nothing in this pass has been verified end-to-end on a physical device.
  - [ ] Apple Sign-In stub now fails loudly instead of silently: since `finalizeOnboarding()` runs at the end of onboarding, picking "Continue with Apple" gets a live session with no OAuth backing, so onboarding finish hits an explicit "Could not finish setup" error. Needs either better error copy or a real Apple implementation before shipping.
  - [ ] Whether Google returns `avatar_url` vs `picture` in `raw_user_meta_data` is unconfirmed — `fn_handle_new_user()`'s `coalesce()` covers both guesses but nobody has seen which key actually fires on a real sign-in.
- [x] Onboarding data persistence — resolved 2026-08-05. `finalizeOnboarding()` (`lib/onboarding/finalize.ts`, called from `all-set.tsx`) writes the full onboarding draft to `profiles` for both the OAuth path (authenticated `updateProfile()`) and the email path (fields ride into `signUp()`'s metadata, since a mid-flow `signUp()` wouldn't yield a session under email confirmation). New `gender`/`onboarding_intent text[]` columns added (`20260805000000_onboarding_profile_fields.sql`); a home-court FK turned out to already exist (`DATA_GAPS.md`'s claim otherwise was stale).
  - [ ] `sign-up.tsx` (a separate, full name/email/password → real `signUp()` screen, unreachable from onboarding) left unreconciled with the onboarding flow's own account creation — two divergent signup implementations still coexist, not merged or retired.
  - [ ] `your-name.tsx`'s pre-fill-from-OAuth-metadata logic untested against a real Google payload — written but unverified end-to-end (blocked on the same real-device test above).
- [ ] Reveal or remove hidden mobile tabs (tournaments, stats, chat, marketplace currently have `href: null`).
- [ ] Decide on mobile dark/light theme support — currently a single fixed palette with no switch, despite web having real dark mode.
- [ ] Remove/guard web's silent mock-tournament fallback (`tournaments/page.tsx`) so backend failures don't look like real empty/normal content.
- [ ] Add real payment processing behind "Hold My Spot" (currently just a DB row, no charge).
- [ ] Post-P0 bug (found 2026-07-24 during ImagePipeline P0 testing): opening Edit Profile while offline may leave the form blank, and a later save could overwrite profile fields with empty values. `fetchProfile` fails offline, logs a console error and returns null, so the screen keeps its empty initial state; a subsequent successful save writes those blanks. Guard the save (or block/retry the form) when the initial profile load failed. Unrelated to ImagePipeline — pre-existing.
- [x] Regenerated `apps/mobile/src/lib/database.types.ts` against the live schema (2026-07-10, as part of the message-attachments migration) — also found and deleted a stray unused duplicate at `apps/mobile/src/lib/supabase/database.types.ts` that nothing imported. Web's `web/src/lib/supabase/database.types.ts` not touched this pass — separate app, not evaluated.

## 8. Design token compliance (defer to Phase 11 — Design Sprint, do not fix mid-feature-work)

- [ ] Mobile: ~57 files with raw hex colors outside `@/theme` (worst: `community/[id].tsx`, `conversation/[id].tsx`, `(tabs)/finder.tsx`, `tournament/[id].tsx`, `(tabs)/marketplace.tsx`, `(tabs)/index.tsx`, `(tabs)/landing.tsx`, `facility/[id].tsx`).
- [ ] Mobile: ~80 files with hardcoded spacing/radius instead of theme constants.
- [ ] Web: 38 files using arbitrary Tailwind bracket values (`text-[13px]`, `p-[17px]`, etc.) instead of token classes.

---

## Freeze (no new feature work, bug fixes only)

- Phase 1 — Tournament Platform (player + admin sides)
- Phase 5 — Facilities Directory
- Phase 7 — Admin Platform (moderation/user/director/settings CRUD only — communications-send and analytics chart above are explicit exceptions, not frozen)
